# revenue-kun v0.1 dependencies
# Policy: guarantee a lower bound and cap fragile core deps below the next major.
#         Avoid over-pinning. Works on Windows / Python 3.11+.
# NOTE: keep this file ASCII-only so `pip install -r` works under Windows cp932.

PyYAML>=6.0,<7
openpyxl>=3.1,<4
pdfplumber>=0.11,<1.0      # core PDF extraction; 0.x can break, so cap it
reportlab>=4.0,<5          # synthetic PDF generation
jsonschema>=4.0,<5         # extraction_log schema validation (tests / optional checks)
pytest>=8.0,<9             # test runner (required by CI)
